# Exceptions

OASIS v0.1 defines no exceptions, privilege levels, interrupts, traps, or
faults.

Reserved classes, reserved opcodes, and nonzero reserved fields are invalid
encodings. Portable software must not rely on any specific invalid-instruction
behavior.

Future OASIS versions may define exception behavior after the Base-16 profile is
fully specified and tested.
